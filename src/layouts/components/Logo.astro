---
import config from "@/config/config.json";
const { src } = Astro.props;
const {
  logo,
  logo_width,
  logo_height,
  logo_text,
  title,
}: {
  logo: string;
  logo_width: any;
  logo_height: any;
  logo_text: string;
  title: string;
} = config.site;
---

<a href="/" class="navbar-brand block">
  {
    src || logo ? (
      <>
        <img
          width={logo_width.replace("px", "") * 2}
          height={logo_height.replace("px", "") * 2}
          src={src ? src : logo}
          alt={title}
          class="dark:hidden"
          style={{
            height: logo_height.replace("px", "") + "px",
            width: "auto",
            maxWidth: logo_width.replace("px", "") + "px",
          }}
        />
        <img
          width={logo_width.replace("px", "") * 2}
          height={logo_height.replace("px", "") * 2}
          src="/images/dark-logo.png"
          alt={title}
          class="hidden dark:block"
          style={{
            height: logo_height.replace("px", "") + "px",
            width: "auto",
            maxWidth: logo_width.replace("px", "") + "px",
          }}
        />
      </>
    ) : logo_text ? (
      logo_text
    ) : (
      title
    )
  }
</a>
